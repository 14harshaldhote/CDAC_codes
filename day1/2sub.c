#include <stdio.h>

int main() {
    int a, b;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    if(a<b){
        for (int i = 1; i <= b; i--){
        a;
    }

    }
    

    printf("Sum: %d\n", a);

    return 0;
}
